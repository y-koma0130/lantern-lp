<section class="cta">
	<div class="cta-inner">
		<h2 class="cta-title">Start tracking your competitors today</h2>
		<p class="cta-subtitle">
			Free plan available. No credit card required. Set up in under 2 minutes.
		</p>
		<a href="#pricing" class="btn-primary-lg">Get Started Free</a>
	</div>
</section>

<style>
	.cta {
		padding: 80px 24px;
		text-align: center;
		background: var(--text-primary);
		color: white;
	}
	.cta-inner {
		max-width: 560px;
		margin: 0 auto;
	}
	.cta-title {
		font-size: 32px;
		font-weight: 700;
		margin-bottom: 12px;
	}
	.cta-subtitle {
		font-size: 16px;
		color: var(--text-tertiary);
		margin-bottom: 32px;
	}
	@media (max-width: 768px) {
		.cta {
			padding: 56px 24px;
		}
		.cta-title {
			font-size: 24px;
		}
	}
</style>
